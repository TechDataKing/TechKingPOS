<Window x:Class="TechKingPOS.App.EditBranchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Branch"
        Width="420"
        Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Border Padding="20" Background="White">
        <StackPanel>

            <TextBlock Text="Edit Branch"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16"/>

            <TextBlock Text="Branch Name"/>
            <TextBox x:Name="NameBox"
                     Height="36"
                     Margin="0,4,0,12"/>

            <TextBlock Text="Branch Code"/>
            <TextBox x:Name="CodeBox"
                     Height="36"
                     Margin="0,4,0,12"/>

            <TextBlock Text="Status"/>
            <StackPanel Orientation="Horizontal" Margin="0,6,0,16">
                <RadioButton x:Name="ActiveRadio"
                             Content="Active"
                             Margin="0,0,12,0"/>
                <RadioButton x:Name="InactiveRadio"
                             Content="Inactive"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Width="100"
                        Margin="0,0,8,0"
                        Click="Cancel_Click"/>
                <Button Content="Save Changes"
                        Width="120"
                        Click="Save_Click"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
