<Window x:Class="TechKingPOS.App.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TechKingPOS Update"
        Width="420"
        Height="280"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="🔄"
                       FontSize="20"
                       Margin="0,0,10,0"/>
            <TextBlock Text="TechKingPOS Update"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Content -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">

            <TextBlock Text="A new version of TechKingPOS is available"
                       FontSize="14"
                       Margin="0,0,0,15"
                       TextAlignment="Center"/>

                <Grid Margin="0,0,0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>


                <TextBlock Text="Current version:"
                            Grid.Row="0"
                            Grid.Column="0"
                           FontWeight="SemiBold"
                           Margin="0,0,10,5"/>

                <TextBlock Grid.Row="0"
                           Grid.Column="1"
                           Text="{Binding CurrentVersion}"
                           Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1"
                            Grid.Column="0"
                           Text="Available version:"
                           FontWeight="SemiBold"
                           Margin="0,0,10,0"/>

                <TextBlock Grid.Row="1"
                           Grid.Column="1"
                           Text="{Binding AvailableVersion}"/>
            </Grid>

            <TextBlock Text="Would you like to update now?"
                       Margin="0,10,0,0"
                       TextAlignment="Center"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">

            <Button Content="Update Now"
                    Width="130"
                    Height="32"
                    Margin="0,0,15,0"
                    IsDefault="True"
                    Click="UpdateNow_Click"/>

            <Button Content="Continue Without Updating"
                    Width="200"
                    Height="32"
                    IsCancel="True"
                    Click="SkipUpdate_Click"/>
        </StackPanel>
    </Grid>
</Window>
