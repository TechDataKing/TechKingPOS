<Window x:Class="TechKingPOS.App.ImportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Preview"
        Height="500"
        Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Alias" Binding="{Binding Alias}"/>
                <DataGridTextColumn Header="Marked Price" Binding="{Binding MarkedPrice}"/>
                <DataGridTextColumn Header="Selling Price" Binding="{Binding SellingPrice}"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Error}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button Content="Import Valid Items" Width="160"
                    Click="Import_Click"/>
        </StackPanel>
    </Grid>
</Window>
